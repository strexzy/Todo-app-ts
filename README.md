# Todo-app-ts ✅

**Todo-app-ts** — простое веб-приложение на React + TypeScript, позволяющее вести список задач: добавлять, отмечать выполненными, удалять и хранить состояние на стороне клиента.

## 🛠️ Технологии

- React
- TypeScript
- Vite
- React Router
- Context, useState
- CSS

## 🚀 Быстрый старт

```bash
# Клонировать репозиторий
git clone https://github.com/strexzy/Todo-app-ts.git

# Перейти в папку проекта
cd Todo-app-ts

# Установить зависимости
npm install        # или yarn

# Запустить приложение в режиме разработки
npm run dev        # или yarn dev

# Открыть в браузере
http://localhost:5173/
```

````

## 📦 Сборка на продакшен

```bash
npm run build      # или yarn build
```

## 📂 Структура проекта (основное)

```
.
├─ public/            # статичные файлы
├─ src/               # исходный код приложения
│   ├─ components/    # React-компоненты
│   ├─ pages/         # страницы
│   ├─ context/       # контекст, state management
│   └─ ...            # утилиты, стили, типы, и др.
├─ package.json
├─ tsconfig.json
├─ vite.config.ts / react-app-config / ... # конфигурации сборщика
└─ README.md         # этот файл
```

## ✨ Возможности (Features)

- Создание новой задачи (todo)
- Отметка задачи как выполненной / невыполненной
- Удаление задачи
- Просмотр списка задач
- Хранение задач в локальном хранилище (localStorage) — чтобы задачи сохранялись между перезапусками страницы

## 🎯 Цель проекта

Проект служит практикой для:

- понимания базовых принципов React + TypeScript,
- управления состоянием,
- взаимодействия с localStorage — чтобы сохранять данные между сессиями,
- структурирования проекта: компоненты, типы, стили, сборка, маршрутизация.
````
